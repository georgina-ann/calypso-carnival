//find the sum of the smallest numbers in array

function sumTwoSmallestNumbers(numbers,int) {  
    //Code here
    numbers.sort(function (a,b){return a - b})
        return (numbers[0] + numbers[1])
      }
  